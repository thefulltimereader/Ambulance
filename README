1. Compile the validator:
   javac Validator.java
2. Solve the problem, write the solution out to a file or stdin
3. run validator with the original data file and result file, or from
   stdin:
   a) java Validator datafile resultfile
   b) executeYourPorgram datafile | java Validator datafile
   
   
   the first argument MUST be the input data file
   the second argmuent can either be a file or standard input
